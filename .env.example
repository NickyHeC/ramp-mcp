# Ramp API Bearer token (used when connecting via DAuth from the client)
# The server receives RAMP_TOKEN through Dedalus Auth (DAuth); the client
# sends this secret when connecting. For local reference or client configuration:

RAMP_TOKEN=your_ramp_bearer_token_here
