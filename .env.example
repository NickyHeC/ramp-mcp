# =============================================================================
# Environment Variables for Ramp MCP Server
# =============================================================================
# Copy this file:  cp .env.example .env
# Then fill in the values below.

# =============================================================================
# Dedalus Platform (required for DAuth)
# =============================================================================

DEDALUS_AS_URL=https://as.dedaluslabs.ai
DEDALUS_API_KEY=
DEDALUS_API_URL=https://api.dedaluslabs.ai

# =============================================================================
# Ramp API Token
# =============================================================================
# The Ramp Bearer token provided by the client via DAuth.
# Must match SecretKeys(token="RAMP_TOKEN") in src/main.py.

RAMP_TOKEN=
